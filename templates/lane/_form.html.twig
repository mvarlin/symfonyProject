{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.lane_name, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.lane_description, {'attr': {'class': 'form-control'}}) }}
    </div>
    <button class="btn {{ button_color|default('btn-outline-primary') }}  mt-3">{{ button_label|default('Save Lane') }}</button>
{{ form_end(form) }}