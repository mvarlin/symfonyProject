{% extends 'base.html.twig' %}
 
{% block title %}New lane{% endblock %}
 
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Create lane</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_lane_index') }}"> 
                    View All lanes
                </a>
            </div>
            <div class="card-body">
                 {{ include('lane/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}